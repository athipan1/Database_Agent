# Migration non-goals

This change does not alter trading decisions, Risk approval, Execution behavior, Backtest promotion thresholds, or broker connectivity. It changes only when database schema mutation occurs and how runtime verifies that the expected schema was deployed.
