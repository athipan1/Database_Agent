# Correlation Propagation

Manager_Agent supplies one correlation ID for each paper reconciliation cycle.
Database_Agent records it on the observation and resulting promotion update so
logs and cross-repository E2E evidence can trace the same cycle through Risk_Agent
and Execution_Agent.
