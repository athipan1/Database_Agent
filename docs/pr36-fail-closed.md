# Fail-closed Behavior

An observation request cannot authorize trading. Any validation, authentication,
version, state, reconciliation, drift, drawdown, expiration, or emergency-halt
failure blocks continued promotion authority and produces an explicit error or
terminal promotion state.
