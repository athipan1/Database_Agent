# PR 36 Audit Trail

Every observation stores the deterministic observation ID, promotion ID, stable
client key, action, reason code, source and destination state and version,
reconciliation counters, timestamps, correlation ID, metadata, and the exact
result snapshot used for idempotent replay.
