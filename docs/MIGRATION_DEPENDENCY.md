# Prerequisite

This branch is based on Database_Agent PR #125 (`agent/backtest-persistence-performance`). Merge or otherwise incorporate that prerequisite before merging the migrations-first startup change into `main`.
