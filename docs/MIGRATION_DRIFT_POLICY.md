# Schema drift policy

Schema drift is an operational error, not a startup repair request. Production startup fails when the canonical identity differs. The deployment migration step owns resolution before the release is allowed to serve traffic.
